name: Autopilot – GitHub Secrets Validator

on:
  workflow_dispatch:
  schedule:
    # Run nightly at 2 AM UTC to catch missing secrets
    - cron: '0 2 * * *'
  push:
    branches:
      - main
    paths:
      - '.github/workflows/autopilot-secrets-validator.yml'
      - 'AUTOPILOT_CHARTER.md'

jobs:
  validate-secrets:
    name: Validate Required GitHub Secrets
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check required secrets exist
        id: check-secrets
        run: |
          echo "# 🔐 GitHub Secrets Validation" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "## Required Secrets Check" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"

          missing=0
          present=0

          # Check NETLIFY_AUTH_TOKEN
          if [ -z "${{ secrets.NETLIFY_AUTH_TOKEN }}" ]; then
            echo "❌ **NETLIFY_AUTH_TOKEN** - Missing" >> "$GITHUB_STEP_SUMMARY"
            echo "   → Add from: Netlify dashboard → User settings → Applications → Personal access tokens" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            missing=$((missing+1))
          else
            echo "✅ **NETLIFY_AUTH_TOKEN** - Present" >> "$GITHUB_STEP_SUMMARY"
            present=$((present+1))
          fi

          # Check NETLIFY_SITE_ID
          if [ -z "${{ secrets.NETLIFY_SITE_ID }}" ]; then
            echo "❌ **NETLIFY_SITE_ID** - Missing" >> "$GITHUB_STEP_SUMMARY"
            echo "   → Add from: Netlify dashboard → Site settings → General → Site details → API ID" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            missing=$((missing+1))
          else
            echo "✅ **NETLIFY_SITE_ID** - Present" >> "$GITHUB_STEP_SUMMARY"
            present=$((present+1))
          fi

          # Check SUPABASE_ANON_KEY
          if [ -z "${{ secrets.SUPABASE_ANON_KEY }}" ]; then
            echo "❌ **SUPABASE_ANON_KEY** - Missing" >> "$GITHUB_STEP_SUMMARY"
            echo "   → Add from: Supabase dashboard → Settings → API → anon public key" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            missing=$((missing+1))
          else
            echo "✅ **SUPABASE_ANON_KEY** - Present" >> "$GITHUB_STEP_SUMMARY"
            present=$((present+1))
          fi

          # Check SUPABASE_SERVICE_ROLE_KEY
          if [ -z "${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" ]; then
            echo "❌ **SUPABASE_SERVICE_ROLE_KEY** - Missing" >> "$GITHUB_STEP_SUMMARY"
            echo "   → Add from: Supabase dashboard → Settings → API → service_role key (click Reveal)" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            missing=$((missing+1))
          else
            echo "✅ **SUPABASE_SERVICE_ROLE_KEY** - Present" >> "$GITHUB_STEP_SUMMARY"
            present=$((present+1))
          fi

          # Summary
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "---" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "## Summary" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "- ✅ Present: $present" >> "$GITHUB_STEP_SUMMARY"
          echo "- ❌ Missing: $missing" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"

          # Set outputs
          echo "missing_count=$missing" >> "$GITHUB_OUTPUT"
          echo "present_count=$present" >> "$GITHUB_OUTPUT"

          if [ "$missing" -gt 0 ]; then
            echo "## ⚠️ Action Required" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "Add missing secrets at: **Settings → Secrets and variables → Actions**" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "After adding secrets, run the **Autopilot – Fix Netlify Environment Variables** workflow to sync them to Netlify." >> "$GITHUB_STEP_SUMMARY"
          else
            echo "## ✅ All Secrets Present" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "All required GitHub Secrets are configured correctly." >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "**Next Step**: Run **Autopilot – Fix Netlify Environment Variables** to ensure Netlify is in sync." >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Validate Supabase URL in repo config
        id: check-supabase-url
        run: |
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "---" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "## 🔍 Repository Configuration Check" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"

          CORRECT_SUPABASE_URL="https://cuxzzpsyufcewtmicszk.supabase.co"
          
          # Check .env.production if it exists
          if [ -f ".env.production" ]; then
            echo "Checking **.env.production**..." >> "$GITHUB_STEP_SUMMARY"
            
            if grep -q "NEXT_PUBLIC_SUPABASE_URL" .env.production; then
              REPO_URL=$(grep "NEXT_PUBLIC_SUPABASE_URL" .env.production | cut -d '=' -f2 | tr -d '"' | tr -d "'" | xargs)
              
              if [ "$REPO_URL" = "$CORRECT_SUPABASE_URL" ]; then
                echo "✅ NEXT_PUBLIC_SUPABASE_URL is correct: \`$REPO_URL\`" >> "$GITHUB_STEP_SUMMARY"
              else
                echo "⚠️ NEXT_PUBLIC_SUPABASE_URL is incorrect:" >> "$GITHUB_STEP_SUMMARY"
                echo "   - Found: \`$REPO_URL\`" >> "$GITHUB_STEP_SUMMARY"
                echo "   - Expected: \`$CORRECT_SUPABASE_URL\`" >> "$GITHUB_STEP_SUMMARY"
                echo "" >> "$GITHUB_STEP_SUMMARY"
                echo "**Note**: Netlify autopilot will override this with the correct value." >> "$GITHUB_STEP_SUMMARY"
              fi
            else
              echo "ℹ️ NEXT_PUBLIC_SUPABASE_URL not found in .env.production" >> "$GITHUB_STEP_SUMMARY"
            fi
          else
            echo "ℹ️ .env.production not found (this is okay - Netlify uses environment variables)" >> "$GITHUB_STEP_SUMMARY"
          fi

          # Check .env.local if it exists
          if [ -f ".env.local" ]; then
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "Checking **.env.local**..." >> "$GITHUB_STEP_SUMMARY"
            
            if grep -q "NEXT_PUBLIC_SUPABASE_URL" .env.local; then
              REPO_URL=$(grep "NEXT_PUBLIC_SUPABASE_URL" .env.local | cut -d '=' -f2 | tr -d '"' | tr -d "'" | xargs)
              
              if [ "$REPO_URL" = "$CORRECT_SUPABASE_URL" ]; then
                echo "✅ NEXT_PUBLIC_SUPABASE_URL is correct: \`$REPO_URL\`" >> "$GITHUB_STEP_SUMMARY"
              else
                echo "⚠️ NEXT_PUBLIC_SUPABASE_URL is incorrect:" >> "$GITHUB_STEP_SUMMARY"
                echo "   - Found: \`$REPO_URL\`" >> "$GITHUB_STEP_SUMMARY"
                echo "   - Expected: \`$CORRECT_SUPABASE_URL\`" >> "$GITHUB_STEP_SUMMARY"
              fi
            else
              echo "ℹ️ NEXT_PUBLIC_SUPABASE_URL not found in .env.local" >> "$GITHUB_STEP_SUMMARY"
            fi
          fi

      - name: Fail if secrets missing
        if: steps.check-secrets.outputs.missing_count != '0'
        run: |
          echo "::error::${{ steps.check-secrets.outputs.missing_count }} required GitHub Secret(s) are missing. Check workflow summary for details."
          exit 1

      - name: Success notification
        if: steps.check-secrets.outputs.missing_count == '0'
        run: |
          echo "✅ All required GitHub Secrets are present and validated."
          echo ""
          echo "🤖 Autopilot Guardian is ready to sync environment variables to Netlify."
