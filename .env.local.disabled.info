# .env.local is DISABLED
# 
# All environment variables are configured in Vercel.
# Local development uses Vercel environment variables via `vercel env pull`.
#
# To re-enable local overrides:
# 1. Rename .env.local.disabled to .env.local
# 2. Add your local overrides
#
# Current setup: All requests go straight to Vercel environment variables.
