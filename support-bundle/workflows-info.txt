=== GITHUB WORKFLOWS ===

Available Workflows:
.github/workflows/advanced-autopilot-inline-check.yml
.github/workflows/ai-agent-autopilot.yml
.github/workflows/ai-agent-claude.yml
.github/workflows/ai-agent-gpt4.yml
.github/workflows/auto-commit-deploy.yml
.github/workflows/autopilot-auto-deploy.yml
.github/workflows/autopilot-autonomous.yml
.github/workflows/autopilot-comment-bridge.yml
.github/workflows/autopilot-fix-skeleton.yml
.github/workflows/autopilot-master.yml
.github/workflows/autopilot-phase2-rollback.yml
.github/workflows/autopilot-simple-deploy.yml
.github/workflows/autopilot.yml
.github/workflows/branch-auto-deploy.yml
.github/workflows/branch-protection-apply.yml
.github/workflows/branch-protection-guard.yml
.github/workflows/cancel-stuck-workflows.yml
.github/workflows/ci.yml
.github/workflows/cloudflare-worker-deploy.yml
.github/workflows/continuous-deploy.yml

=== VERCEL DEPLOYMENT WORKFLOW ===
name: Vercel Production Deployment

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
  VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.11.1'

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.7.0

      - name: Get pnpm store directory
        id: pnpm-cache
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path)" >> $GITHUB_OUTPUT

      - name: Setup pnpm cache
        uses: actions/cache@v4
        with:
          path: ${{ steps.pnpm-cache.outputs.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install Vercel CLI
        run: pnpm add -g vercel@latest

      - name: Pull Vercel Environment Information
